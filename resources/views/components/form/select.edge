<select class="w-full p-1 border border-gray-3 rounded-md" name="{{ name }}" x-model="{{ name }}">
  @each(item in items)
    <option
      value="{{ item.value }}"
    />
      {{ item.value }}
    </option>
  @endeach
</select>
